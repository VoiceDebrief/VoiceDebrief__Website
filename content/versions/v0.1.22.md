---
version: v0.1.22
date: 2026-08-07
title: The workflow becomes a declared state machine, and the review pack lands
---

- The one-pass now executes from a declared JSON state machine (`workflows/standard.json`, issues 042–043): per-step models, spending ceilings, transitions and failure behaviour; a validator, a budget gate at every step boundary, the max cost quoted before anything runs, and the 🧭 flow panel rendering the declaration and the live execution trace
- The review pack's programme executed: the public record fixed (group A), CSP on the app page (037), the open engineering hub at /engineering/ (036), and QA-to-docs — journeys that screenshot-QA the product (038, first slice)
- `?origin=` removed entirely (issue 041): the engine origin is hardcoded; a URL parameter no longer decides which JavaScript runs beside the key
- Each estate's derived links now point at their own branch (`build_content.py --ref`), fixing three honest 404s the live-QA link check caught; every CI job carries an explicit timeout
