# Prioritised Recommendations

**version** v0.1.20
**date** 6 August 2026
**from** Claude (review session agent)
**type** Review — consolidated recommendations

*Part of the [project library](https://whatsapp-voice-transcription.sgraph.ai/library/) — every doc behind this product, organised by audience.*

---

Every recommendation from docs 01–06, deduplicated and ordered. Groups A–C are
sequenced (A before B before C where they touch); D–F can run in parallel with
anything. **§H lists the four decisions only Dinis can make** — three of them are the
actual bottleneck of the open queue.

## A. Fix The Record (one focused session; do first)

| # | Action | Source |
|---|--------|--------|
| A1 | versions.json → v0.1.19/v0.1.20; split the changelog's Unreleased block into its real tag headings | 04 §2, 05 §2 |
| A2 | Fix the 404'd issue-034 link; repoint `blob/qa` issue links to `blob/dev/issues/done/`; pin the two moving `compare/...dev`/`...qa` links to tags | 05 §2 |
| A3 | Reality doc: fix the M3 PROPOSED contradiction + header date; `issues/done/README.md` header; close out the two dev packs' PROPOSED status lines | 04 §2 |
| A4 | Re-date/re-scope all open-issue status notes (frozen at 28 Jul); nudge-or-close issue 007 | 04 §5 |
| A5 | Landing copy: remove "Pick your privacy mode" step; fix the beta tense; label the pricing section's planned half PROPOSED | 05 §2–3 |
| A6 | Add outbound link-checking of Updates/Library to the live-QA job so A2's class can't recur | 05 §3 |

## B. Security Hardening (before the seeded key; B1–B2 this week)

| # | Action | Source |
|---|--------|--------|
| B1 | **Allowlist `?origin=`** (~5 lines) — closes the remote-code / key-exfiltration link vector on the live app | 02 S1 |
| B2 | Meta CSP on the app page (`script-src`/`connect-src` pinned) | 02 S2 |
| B3 | Confirm-gate or de-scope model-callable `set_prompt`/`reset_prompt` (persistence is the prompt-injection prize); consider confirm-gating `update_transcript` | 02 S3 |
| B4 | gitleaks (or trufflehog) in CI + pre-commit hook — the enforcement issue 013 has awaited since 28 Jul | 03 §3.1 |
| B5 | AppSec seeded-key conditions doc (cap, lifetime, revocation, allowlist, transition trigger) + SECURITY.md with disclosure contact | 04 §4 |
| B6 | Privacy notice, published on the site (doubles as the missing trust page) | 04 §4, 05 §3 |
| B7 | Verify/sanitise model-generated SVG (`on*`, `<foreignObject>`); rename the `-mgmt-` localStorage key with migration; validate key on save via `getKeyStatus`; escape the model name in `infographicNote` | 02 S4–S5 |

## C. The Open Engineering Hub (issue 036; after A so it renders a true record)

| # | Action | Source |
|---|--------|--------|
| C1 | M-hub-1: CI emits `status.json` + `issues.json`; `/engineering/` hub page renders them (the `/versions/` pattern) | 06 |
| C2 | M-hub-2: the five section pages (pipeline, testing, docs, security, team); security page doubles with B6 | 06 |
| C3 | M-hub-3: the "You vibe-coded a service. Now what?" series via Updates; OG tags + RSS feed so it travels | 06, 05 §3 |

## D. Testing (medium; interleave with product work)

D1 hostile-content render tests (`wa-result-card.show()`, `wa-chat-panel.md()`, malicious-transcript case) · D2 image-infographic path test (the untested default) · D3 budget-exhaustion test (also fixes the spendUsd accounting gap, 02 §3) · D4 failure-injection (401/402/429 → ERROR_COPY, `diagnoseLlmFailure`) · D5 add `chat-prompt.md` to live-QA (one line) · D6 `.m4a` fixture or an honest note in tests/README · D7 move/generalise the hardcoded-path scripts in `tests/playwright/`.

## E. CI/CD (cheap hardening)

E1 pin `OSBot-GitHub-Actions` to a SHA; pin playwright / commit a lockfile · E2 ESLint in the test job · E3 replace checkout-then-pull with tag-SHA-exact checkout · E4 mirror/pin the engine for the integration gate (MIRROR_DIR); keep live origin as non-blocking canary · E5 weekly spend-capped keyed canary · E6 drop `m1-spike-test.html` from the production deploy.

## F. Code Quality And Docs (background pass)

F1 shared `wa-shared` util module (esc, tool-poll, grip-resize, ticker, one markdown renderer) · F2 deduplicate `CHAT_MODEL_DEFAULT` / key-name constants; `ask()` delegates to `chat()` · F3 one source of session-cost truth · F4 architecture-as-built doc + API reference for the `whatsapp-transcribe` actions and 13 chat tools · F5 minimal runbooks (deploy failure/rollback, key revocation drill, QA secrets) + incident-response note formalising the issue-025 pattern · F6 CONTRIBUTING.md (three lines pointing at the guide) · F7 CC BY footer sweep; standardise the GitHub org name in links.

## H. Decisions Only Dinis Can Make

| # | Decision | What it unblocks |
|---|----------|------------------|
| D1 | Personal vs organisation OpenRouter account | Issues 010 + 014 — the whole key/guardrails stream |
| D2 | The model allowlist for the app | The guardrail configuration and B5's conditions doc |
| D3 | Code licence: bless the Apache 2.0 LICENSE or replace it | The repo's self-contradictory legal status; a contract open point |
| D4 | `main` topology: wire a production deploy, or amend the public "two estates" story (and stop major-tagging `main` until real) | 03 §3.6, 05 §2.7 |

---

This document is released under the Creative Commons Attribution 4.0 International licence (CC BY 4.0).
