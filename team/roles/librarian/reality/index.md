# Reality: What Exists Today

*Part of the [project library](https://voicedebrief.ai/library/) — every doc behind this product, organised by audience.*


**maintained by** Librarian
**updated** 2026-08-12
**rule** If it is not in this document, it does not exist. Proposed items are labelled
`PROPOSED — does not exist yet`.

---

## EXISTS (verified in this repo)

| Item | Where | Since |
|------|-------|-------|
| Repo scaffold (README, LICENSE, .gitignore) | repo root | initial commit |
| Brief pack: session understanding, architecture, commercial model, task plan, source map | `library/brief-pack/` | 2026-07-28 |
| Agentic team wiring: rulebook, roster, 10 active ROLE files, humans/comms folders | `.claude/CLAUDE.md`, `team/` | 2026-07-28 |
| This reality document | `team/roles/librarian/reality/index.md` | 2026-07-28 |
| CI: auto-tag on `dev`/`main`, then Pages publish with version stamped into the site | `.github/workflows/ci-pipeline.yml` | 2026-07-28 |
| Issues tracking (issues-fs-lite style) | `issues/` | 2026-07-28 |
| Project library with imported source docs | `library/` | 2026-07-28 |
| MVP static site (footer shows `version`; published after tagging) | `website/` | 2026-07-28 |
| Tech stack & workflow guide for incoming devs | `library/guides/v0.1.0__guide__tech-stack-and-workflow.md` | 2026-07-29 |
| `pyproject.toml` (version CI-owned; unblocks increment-tag) + root README with release badge and repo map | repo root | 2026-07-29 |
| Issues READMEs: top-level table + per-folder views (maintained manually per commit) | `issues/*/README.md` | 2026-07-29 |
| Dev pack: audio-transcribe integration briefs (the work they proposed shipped as M1–M2, issues 008/024) | `library/dev_packs/v0.1.1__audio-transcribe-integration/` | 2026-07-29 |
| GH Pages + Route 53 DNS guide | `library/guides/v0.1.1__guide__github-pages-and-route53-dns.md` | 2026-07-29 |
| **Domain: `voicedebrief.ai` (prod) and `qa.voicedebrief.ai` (QA)** — live and serving. The former hosts `whatsapp-voice-transcription.sgraph.ai` / `qa.…` have **no DNS**, so every absolute URL naming them is dead; swept out of the published surface, its generators, the page templates, `llms.txt`, `sitemap.xml`, `robots.txt`, the RSS feed's links and live-QA's default `LIVE_URL` on 12 Aug. The dated record (library, reviews, debriefs, done issues, CHANGELOG) still names the old host, deliberately — it describes what was true then | `website/`, `scripts/`, `tests/qa/live-site-check.mjs` | 2026-08-12 |
| Custom domain LIVE: Route 53 CNAME + Pages claim, HTTPS enforced, HTTP 301s, version-stamped (issue 021 closed) | https://voicedebrief.ai | 2026-07-29 |
| Journalist role (active): owns the public record — 11 Updates posts, the Versions list and the Videos page, all authored as markdown under `content/` (one file per post; see issue 039) | `team/roles/journalist/`, `content/updates/` | 2026-07-29 (posts current to 2026-08-06) |
| Public Library page (Librarian-maintained doc front door) | `website/library/` | 2026-07-29 |
| CHANGELOG.md + Librarian-Journalist per-version capture discipline | `CHANGELOG.md`, both ROLE files, `.claude/CLAUDE.md` | 2026-07-29 |
| Three product infographics (product overview, technical architecture, user journey) — created for this repo. The seven strategy-level ones from 28 Jul never landed; issue 007 closed as superseded (Dinis, 6 Aug) | `library/infographics/` | 2026-07-31 |
| M1-a spike harness: browser page that runs Attempt 1 (entry module) vs Attempt 2 (builders on our own `SgToolApi`) in isolated iframes and reports the contract surface each publishes. Dev harness only — no product code, and the spike's verdict note is not written yet | `website/m1-spike-test.html` | 2026-08-03 |
| The product app (beta): THE FULL ONE-PASS — drop → transcript → summary → infographic (streamed SVG via reused `sg-llm-infographic`, save .svg), BYOK key, GBP costs, own `whatsapp-transcribe` SgToolApi. Verified e2e 5 Aug (real key + real note): transcript 5.3s, summary 14.7s, infographic 53s, £0.003/pass, zero errors. Issues 008+024 closed | `website/app/`, `website/components/` | 2026-08-05 |
| sg-llm-infographic verified capabilities guide (one-call shape, cell wiring, surface, sanitisation) | `library/tools/infographic-generator/` | 2026-08-05 |
| Content-based audio normalisation (`OggS`/`OpusHead` sniffing) closing the `.ogg` hallucination bug — issue 025, verified across six file/MIME variants; upstream reported to the Tools team | `website/app/audio-normalise.js`, `team/comms/briefs/08/05/` | 2026-08-05 |
| CI cache-busting: every deploy stamps `?v=<version>` onto same-origin JS/CSS so a browser cannot run last deploy's modules against new HTML (issue 026); infographic failures now surface a message | `scripts/stamp_cache_busters.py`, `.github/workflows/ci-pipeline.yml` | 2026-08-05 |
| Spike verdict recorded: Attempt 2 (dev pack brief 06); engine harness in `website/app/engine.js` | dev pack 06 | 2026-08-05 |
| Two genuine WhatsApp `.opus` fixtures (laptop-download case) + Playwright smoke script | `tests/` | 2026-08-05 |
| Advanced/debug pane: `wa-debug-panel` v0.1.0 (resizable right-edge pane — LLM exchange log with full request/response, OpenRouter key/model/generation lookups, editable prompt templates with localStorage overrides incl. the engine's transcription prompt via our transport wrapper); capture layer `debug-store.js`; 8 new API actions; verified e2e with a real key (issue 027) | `website/app/debug-store.js`, `website/app/openrouter.js`, `website/components/wa-debug-panel/` | 2026-08-05 |
| Sample voice notes clickable in the UI (load + auto-run with a saved key) | `website/app/samples/`, app page chips | 2026-08-05 |
| CI test gate: unit (22, node --test) + integration (11-check Playwright app boot) before tag/publish; post-deploy live-site QA job (issue 028) | `tests/unit/`, `tests/integration/`, `tests/qa/`, `ci-pipeline.yml` | 2026-08-05 |
| Repeat-pass fix: same-file rerun reuses the engine's deduped queue item instead of failing "not-audio" (issue 029; upstream reported) | `website/app/pipeline.js`, `team/comms/briefs/08/05/` | 2026-08-05 |
| QA estate LIVE: `qa` branch → Netlify (test gate → stamped `-qa.<sha>` build → netlify-cli publish → 16-check live verification). Secrets in place; verified serving v0.1.14-qa.cbd4382 (issue 030 closed) | `.github/workflows/qa-deploy.yml`, branch `qa`, https://qa.voicedebrief.ai | 2026-08-05 |
| Infographic v2 (issue 031): default model `google/gemini-3.1-flash-image-preview` returns a finished image; model picker + redraw on the card (`redrawInfographic` action); editable infographic system prompt (4th debug-pane template); spinner + elapsed heartbeat in main UX and debug pane v0.1.1; samples load into the options screen. Verified e2e (image ~850KB + SVG redraw, real key) | `website/app/infographic.js`, `website/components/wa-debug-panel/v0/v0.1/v0.1.1/` | 2026-08-05 |
| Disabled-key diagnosis (issue 032): network-shaped LLM failures re-checked via GET /api/v1/key; rejected keys named in the error card and on the key panel | `website/app/app.js` | 2026-08-05 |
| Versions page (issue 033): per-tag timeline with diffs (`versions.json` kept in step with CHANGELOG); version chip on every page footer; QA builds stamped `<next-version>-qa.<sha>` from the tags | `website/versions/`, both workflows | 2026-08-05 |
| M3 chat LIVE (issue 034): 💬 `wa-chat-panel` + `chat.js`/`chat-tools.js` — context composer, fenced-block tool loop (typed registry, 3 budgets), debug-audited; validated live by Dinis (tool-driven exchanges, and the chat authored the issue-035 brief) | `website/app/chat*.js`, `website/components/wa-chat-panel/` | 2026-08-05 |
| `?origin=` REMOVED (issue 041): the engine origin is a hardcoded constant; the parameter previously let any URL decide which JavaScript ran in the page beside the user's OpenRouter key. Tests assert `config.js` never reads the query string. Briefing guide kept | `website/app/config.js`, `library/guides/v0.1.20__guide__the-origin-parameter.md` | 2026-08-06 |
| Content architecture (issue 039): `content/updates|versions|videos/*.md` are the source of truth; `scripts/build_content.py` renders the Updates + Videos pages, `updates.json`, `videos.json`, `feed.xml` and `versions.json` as gitignored build output; links derived from `version:`/`issues:`; validation gates both CI workflows | `content/`, `scripts/build_content.py`, `scripts/templates/` | 2026-08-06 |
| Videos page + content type (issue 040): `/videos/`, grouped by kind, click-to-load players (no third-party request before play, asserted by test); three real videos published (chat feature, QA tour, first MVP), a fourth held as a draft pending its id | `website/videos/` (generated), `content/videos/` | 2026-08-06 |
| Chat material edits + image vision (issue 035): `updateMaterial`/`restoreMaterial` actions, `update_transcript`/`update_summary`/`restore_original`/`view_infographic` tools (13 total), revert note under the cards, infographic image as a multimodal part (composer row + tool attachment), suggestions only on a blank thread (`wa-chat-panel` v0.1.1) | `website/app/pipeline.js`, `chat*.js`, panel v0.1.1 | 2026-08-06 |
| Review pack v0.1.20: full project review (code/security, testing+CI, docs, live site) + two proposals — the open engineering hub (doc 06) and QA-to-docs screenshot-verified user docs (doc 08, pattern from Dinis); issues 036 (hub), 037 (security hardening, urgent) and 038 (QA-to-docs) opened from it | `library/review-packs/v0.1.20__project-review/` | 2026-08-06 |
| Security hardening, first slice (issue 037): meta CSP on the app page pinning script/connect sources — verified against the full real decode chain and both integration gates. (Item 1's `?origin=` allowlist was superseded same-day by issue 041's removal of the parameter; the CSP's localhost allowances went with it) | `website/app/index.html`, `website/app/config.js` | 2026-08-06 |
| Engine mirror builder for the integration tests' MIRROR_DIR mode (sandboxed/pinned runs — review E4): crawls the app's tools-origin imports recursively, 32 modules | `scripts/mirror_engine.mjs` | 2026-08-06 |
| Live-QA outbound link check: GitHub + same-origin links on Updates/Library must resolve (third-party hosts warn only) | `tests/qa/live-site-check.mjs` §6 | 2026-08-06 |
| The open engineering hub (issue 036, M-hub-1+2): `/engineering/` + five section pages (pipeline/testing/docs/security/team) rendering CI-emitted `status.json`/`issues.json`/`docs.json`; both workflows emit at deploy (test results travel by artifact); hub pages in the live-QA check | `website/engineering/`, `scripts/emit_engineering_json.py`, both workflows | 2026-08-06 |
| Story-travel plumbing (M-hub-3 slice): OG/Twitter tags on landing, Updates and the hub; RSS feed of Updates posts (Journalist-maintained per post) | `website/updates/feed.xml`, page heads | 2026-08-06 |
| The workflow DECLARED (issue 042, human brief v0.33.56): `runPass` executes from `website/app/workflows/standard.json` — a JSON state machine (requires/produces/model/budget/next/on_failure per step) with a validator, quote maths and a runner enforcing budget entry gates; the quotable max shown pre-run; execution trace on `results.trace` + `wa:workflow:*` events; deleting the file breaks the tool (no code fallback). 7 unit tests | `website/app/workflow.js`, `website/app/workflows/`, `tests/unit/workflow.test.mjs` | 2026-08-06 |
| The 🧭 flow panel (issue 043): third right-edge tab rendering the declaration (steps, models, ceilings, branch, quote) and the live execution trace (status, cost vs ceiling, durations, overruns flagged); joins the one-pane protocol; app-boot checks + qa-to-docs shot 08-flow-run | `website/components/wa-flow-panel/v0/v0.1/v0.1.0/` | 2026-08-06 |
| Workflow dev pack: the v0.33.56 human brief grounded in this codebase — generic format / minimal machinery split, honest budget semantics, the D2 constraint on consensus (needs a second audio-model family) | `library/dev_packs/v0.1.21__workflow-state-machine/` | 2026-08-06 |
| Single-source site nav (issue 048): `wa-site-nav` v0.1.0 — dependency-free web component, all 12 pages/templates, path-derived active state, engineering sub-row, BETA badge on the app; the five drifted header variants retired | `website/components/wa-site-nav/` | 2026-08-07 |
| Nav v0.1.1 (issue 054): second level — 6 primary links + News ▾ (Updates/Versions/Videos) and Engineering ▾ (6 sections) CSS-only dropdowns; ≤760px a hamburger opens a grouped panel (v0.1.0 hid the menu entirely); all 17 refs flipped; browser suite pins the structure (12 tests) | `website/components/wa-site-nav/v0/v0.1/v0.1.1/` | 2026-08-08 |
| Product name analysis (Dinis's request — decision pending, his): the five "WhatsApp …" candidates fail on the Meta mark + SERP head owned by WhatsApp's built-in transcripts; after two rounds (VoiceBrief withdrawn — active namesake at voicebrief.io) the working name is **VoiceDebrief for WhatsApp** (Dinis, 8 Aug); registrar + trademark verification pending, subdomain unchanged. `PROPOSED — no renaming done` | `library/briefs/naming/v0.1.23__analysis__product-name-and-domain.md` | 2026-08-08 |
| Run-visibility fix (issue 046): page resets cancel an in-flight pass; a completed pass re-shows its results if the sections were hidden — found live by Dinis via the flow panel's trace outliving the page state | `website/app/app.js` | 2026-08-07 |
| LLM-friendly + SEO (issue 047): `/llms.txt` (skills docs, manifest, workflow declaration, all machine-readable surfaces indexed), generated `/sitemap.xml`, canonicals to production on every page, JSON-LD (SoftwareApplication + Blog), QA estate noindexed at deploy, live-QA coverage | `website/llms.txt`, `scripts/build_content.py`, page heads, `qa-deploy.yml` | 2026-08-07 |
| QA-to-docs harness (issue 038, M-qtd-1): 2 journeys / 8 manifest-named shots with masks, deterministic scripted-OpenRouter replay, pixelmatch diff gate (0.000% self-diff verified; perturbation caught at 0.954%); gating in both workflows with candidates/diffs as the `qa-to-docs` artifact | `tests/qa-to-docs/`, both workflows | 2026-08-06 |
| Per-shot framing (`clip` in `journeys.json`): absent = whole page, `viewport` = first screen, otherwise a CSS selector (shadow DOM pierced, e.g. `wa-chat-panel .wa-chat__panel`). Full-page captures buried the subject under the hero and made the diff surface the entire page; done before any baseline was armed, so nothing needed re-arming | `tests/qa-to-docs/run.mjs`, `journeys.json` | 2026-08-07 |
| The user guide (issue 038, M-qtd-2): `/user-guide/` generated from `journeys.json` + the committed baselines — three sections, one figure per shot, captions from the manifest. A shot with no baseline renders an honest "not captured yet" placeholder, never a broken image. Linked from every page's nav; in the live-QA check | `scripts/build_user_guide.py`, `scripts/templates/user-guide.html`, `tests/unit/user-guide.test.mjs` | 2026-08-07 |
| One real URL per language (issue 056): `/app/en-gb/`, `/app/en-us/`, `/app/pt-pt/`, `/app/pt-br/` — real directories generated by `scripts/build_locale_pages.py` from the allowlist (gitignored build output), because GitHub Pages cannot rewrite and a path rescued by JavaScript is not a shareable link. Each carries `<base href="../">`, BCP-47 `lang`, a self-canonical and the full hreflang set + x-default. Precedence: URL → stored → navigator (live only) → default; the URL must beat a stored choice or shared links fail for exactly the people sent them. Switching rewrites the bar with `replaceState`, no reload. The path becomes an ALLOWLIST KEY, never a fetched URL (the issue-041 distinction). Gated both ways in CI + live-QA | `scripts/build_locale_pages.py`, `website/app/i18n.js` | 2026-08-08 |
| Translate before summarising (issue 055): a `translate` step in the declared workflow between transcribe and summary, ON by default, conditional on `options.translate`. The transcript is NEVER overwritten (it is the record of what was said); the translation is its own material with its own card, and **the summary and infographic are both built from it**. Declared budget 0.03 so the ceiling stays quotable; `degrade` on failure so a lost translation never costs the transcript. Prompt is a site file with `{{language}}`/`{{tone}}` from culture data, editable as a 4th debug template. `wa-progress-rail` v0.1.4 adds the row. Gate: `tests/integration/translate.test.mjs`, 7 checks, keyless, both pipelines. NOT built: language detection — a note already in the reader's language still costs one call | `website/app/workflows/standard.json`, `website/app/pipeline.js`, `website/app/prompts/translate-prompt.md` | 2026-08-08 |
| Semantic-graphs briefing pack (issue 058, `PROPOSED — no extraction or mapping is built`): `library/briefs/semantic-graphs/` — a self-contained, zippable pack (12 files, ~26k tokens) so an interactive session with no repo access can produce a markdown simulation of concept graphs over a realistic voice-note corpus, which then becomes the visualisation spec. Holds both of Dinis's 6 Aug briefs verbatim, what issue 057 actually built, the hard constraints, the real `concepts.json` / prompts / workflow as data, and an ask with acceptance criteria. Engineering review recorded three corrections: windowing is the ADJUDICATION batch over retrieved candidates, not a sweep of the scheme (the sweep reading costs 100 calls per recording on a 500-concept ontology and cannot be quoted against our $0.30 ceiling); embedding retrieval is unavailable with no backend, so lexical retrieval over label+altLabel+definition+scopeNote substitutes; and format symmetry means one core schema with TWO profiles, since a customer ontology needs provenance and not verdicts. Also flagged: a voice note yields 3-8 concepts where the cited research indexes documents yielding dozens, so bootstrapping likely needs 20-50 recordings — the main thing the simulation must measure | `library/briefs/semantic-graphs/` | 2026-08-08 |
| Concept scheme + English review pass (issue 057, from Dinis's *Concepts, Not Words* brief): `/engineering/concepts/` — 15 concepts in 3 schemes (locale/culture, privacy modes, artefacts), each with a definition, scope note, the label shipping today in all four cultures, a verdict and proposed labels. SKOS **vocabulary** (definition/scopeNote/broader/narrower/related), NOT the RDF stack — no triple store, no SPARQL, which for 15 concepts on a backend-less site would be all cost. Scope is TERMS not strings: 53 keys ship and most are prose; a sentence has meaning in context, not a concept. **Deliberately NOT wired** — the app reads nothing from it. Three views (cards / matrix / relations graph). Findings: `draft`→`unreviewed` (all four locales ship 53/53 keys, so the concept is a state of VERIFICATION, not authoring or coverage — the brief's own guess of "incomplete" is ruled out by the data, and `rascunho` faithfully carried a word already pointing at the wrong axis); `soon`→`planned` (en-gb's own culture.json forbids marketing register); the two privacy chips name a mechanism inside a group asking where the audio goes, marked `decide` pending sign-off because they are claims to users. Drift gate `tests/unit/concepts.test.mjs` asserts every recorded label against the real locale files — proven to fail when a string moves. Held-out generation check NOT built and will be a report, not a gate (model-judged, cannot separate a defect from a lexical gap — issue 053's precedent) | `website/engineering/concepts/`, `tests/unit/concepts.test.mjs` | 2026-08-08 |
| Keyless demo + collapsing key panel (issue 063): `runPass({demo:true})` runs the REAL declared workflow, executor shape, wa:* stream, trace and cards with only the model answers substituted — not a simulation, because a demo that drifts from the product lies first about what it exists to show. Gated by `tests/integration/demo.test.mjs` (10 checks) which sets NO key and ABORTS any OpenRouter request rather than mocking it, so touching the network fails the build. Every demo artefact sits under a DEMO stamp and a real pass clears it. The infographic is deliberately NOT faked (a canned image of someone else's data would be the most misleading thing on the page) — that step degrades with a reason. `wa-key-panel` v0.1.3 collapses to one line once a key is saved, reopens on `change`, never redisplays the key, and REOPENS on a rejected key because collapsing on failure hides the problem behind a tick. Three bugs traced to one cause: the demo button borrowed `.sample-chip` for styling and three things selected on it — all now qualify on `[data-sample]` | `website/app/demo.js`, `website/components/wa-key-panel/v0/v0.1/v0.1.3/` | 2026-08-08 |
| Metadata step + conditional translation (issue 061): a `classify` step on EVERY pass reads the transcript once and returns TYPED facts — detected language+confidence, ≤5 topics, register, sentiment, urgency, safety signals, one-line gist — and the declaration guards translate on `facts.needsTranslation`, so a note already in the reader's language is not translated into itself (verified: $0.0015 spent against a $0.11 quote). The state machine gains a `facts` namespace with `quoteWhen`: a fact resolves TRUE when QUOTING (the ceiling assumes every discoverable branch) and for real at RUN time, so a pass may spend less than quoted and never more. Two invariants in the runner, not convention: **a fact may never add a step** (a transition to an unquoted target is unavailable however true), and **a degrading step may still declare facts** (classify attaches needsTranslation:true to its own failure, because undefined reads as false and that is the dangerous direction). `classify.js` is a trust boundary: every value is allowlisted, NOTHING is ever put back into a prompt, and unsure means translate. Signals include prompt-injection, credentials, personal-data, financial-request, urgency-pressure, legal-or-medical — the last two together being the shape of voice-note fraud. The signals are labelled a SIGNAL NOT A CONTROL on the card itself: the classifier reads the same untrusted text and can be talked out of reporting | `website/app/classify.js`, `website/components/wa-facts-card/`, `tests/unit/classify.test.mjs` | 2026-08-08 |
| The public Library page is gated (`tests/unit/library-page.test.mjs`): every folder under `library/briefs/` must be linked from `website/library/index.html` AND from `library/README.md`, and every link must name a branch that publishes (dev or qa). Written because the rule — already in CLAUDE.md — failed twice in one day: the go-live brief and the semantic-graphs pack were both added to the repo and to the repo index and neither reached the page a visitor sees, until Dinis hit a 404 looking for one. The gate found a third gap on its first run (the naming brief was missing from the repo index) | `tests/unit/library-page.test.mjs`, `website/library/index.html` | 2026-08-08 |
| Browser suite enforces QUnit's global-pollution check (issue 049): `QUnit.config.noglobals = true` is set on the page, not left to the toolbar checkbox that defaults OFF — the locale-picker tests stubbed `window.__waI18n` and restored it as `undefined`, which leaves the PROPERTY defined, so the suite went red for anyone who ticked the box and stayed green in CI. The hook now deletes the property instead. A gate that is only strict when a human ticks something is not a gate; page and CI now enforce the same rule, verified by reintroducing the leak | `website/tests/browser/index.html`, `website/tests/browser/tests.mjs` | 2026-08-08 |
| Go-live design brief (issue 060, `PROPOSED — the redesign does not exist yet`): `library/briefs/go-live/` for Claude Design — workflow onto the home page, the three privacy chips DELETED and replaced by a plain statement that audio+transcripts go to OpenRouter and we cannot name or control the provider (two of the three chips never existed; a privacy selector implies a control we do not have), a Gmail-style beta mark, an LLM caveat, an OpenRouter key guide page, and the rename to **VoiceDebrief for WhatsApp** / VoiceDebrief.ai (43 hardcoded strings + the title key in 4 locales). Records the constraints as non-negotiable: no backend, no SPA, colour only via --wa-* tokens, strict CSP, four cultures with pt running 15-25% longer, phone-first, a11y. The brief was answered on 12 Aug and **the answer is built** — see the next row | `library/briefs/go-live/`, `issues/open/060-…` | 2026-08-08 |
| Artefact language declared, not hardcoded (issue 055 fix): `summary-prompt.md` ended with "British English", so a pt-PT reader got a correct Portuguese translation and then an ENGLISH debrief built from it. Both the summary and infographic prompts now carry `{{language}}`/`{{tone}}` substituted from the reader's culture, as translate already did; `runPass` keeps `options` on `current` so an infographic redraw (no step ctx) is in the right language too. The gap in the gate mattered as much: the test asserted the REPLY was Portuguese and passed because the mock answers in Portuguese whenever the request mentions Portuguese — it now asserts the outgoing PROMPT names the reader's language and no longer says "British English" | `website/app/prompts/`, `website/app/pipeline.js`, `tests/integration/translate.test.mjs` | 2026-08-08 |
| Nav link colour regression fixed (`wa-site-nav` v0.1.6): wrapping the App link in `.i18n-link` for its locale flag (v0.1.3) took it out of `nav.main > a`, so it painted browser-default blue — purple once visited — on the navy bar. Selectors styling top-level links must name both shapes; browser test compares computed colours | `website/components/wa-site-nav/v0/v0.1/v0.1.6/` | 2026-08-08 |
| The locale panel was NEVER closed (Dinis, screenshot, issue 050): from v0.1.1 to v0.1.3 the culture list rendered on every page load on every viewport. `toggle()` sets `panel.hidden`, which closes an element only via the UA rule `[hidden]{display:none}` — and the component's own `.panel{display:grid}` is an AUTHOR rule, so it won. Measured: `hidden` property true, `hasAttribute` true, **computed display `grid`, box 612x328**. `wa-locale-picker` v0.1.4 adds `.panel[hidden]{display:none}` with the reasoning inline. The durable lesson is about verification, not CSS: every check asked `panel.hidden`, a property that faithfully reports what the code assigned and has NO relationship to what is painted — so the probes, the assertions and the report to Dinis all agreed with each other and all described something invisible to the browser. It also produced a wrong answer that cost a day ("the panel does NOT auto-open — that was a tap"; it was not a tap). Rule adopted: **visibility is asserted from computed style or a box, never from the attribute meant to cause it**. Three browser tests apply it; removing the one-line fix fails two of them | `website/components/wa-locale-picker/v0/v0.1/v0.1.4/`, `website/tests/browser/tests.mjs` | 2026-08-08 |
| The picker survives a phone (Dinis, iPhone screenshots, issue 050): on a narrow screen the header wrapped, `space-between` threw the picker to the far LEFT of row two and the hamburger to the far right, and the panel — anchored `right:0` to the trigger — opened at **x=−131px** with 60px of the list below the fold (reproduced at 390×664). `wa-site-nav` v0.1.4 puts the locale slot and the hamburger in one `.right` cluster (`margin-left:auto`, `flex:0 0 auto`) so they stay adjacent and hard right whichever row they land on; the brand shrinks then ellipses before they do. `wa-locale-picker` v0.1.3 stops being a dropdown below 620px: the panel is `position:fixed`, inset 10px from both screen edges, `max-height:calc(100vh - 120px)` with scroll, top set from the trigger's rect in `place()` and re-placed on `resize` (a rotation must not discard an open panel). Both buttons go flag-only below 560px with the code moved into `title`/`aria-label`. The BETA badge deliberately KEPT — hiding it buys a one-row header, but what it says is worth more than 32px. Verified fully on screen with no horizontal scroll at 320/390/1280; pinned by a browser test on the cluster's structure and right-flush position | `website/components/wa-locale-picker/v0/v0.1/v0.1.3/`, `website/components/wa-site-nav/v0/v0.1/v0.1.4/` | 2026-08-08 |
| The picker moves to the nav, top right (Dinis, issue 050): `wa-locale-picker` v0.1.2 is slotted into `wa-site-nav` v0.1.3 through `<slot name="locale">` — slotted, not built in, so the twelve pages with no i18n are untouched. The panel is CLOSED until the caret is clicked. Once a non-default locale is active a plain 🇬🇧 **EN-GB** button appears beside the dropdown: one click home, no menu — someone who lands on Portuguese from a shared link should not have to operate a dropdown they cannot read to undo it. The trigger shows the locale CODE (🇵🇹 PT-PT), not the native name, which wrapped the control onto a second row and dragged the panel off-screen left. The nav also states what is translated: `/app/` is listed first with the ACTIVE locale's flag, English-only links sit behind one 🇬🇧 marker (constant `TRANSLATED`, one entry today). `wa-site-nav` gained a re-runnable `render()` hooked to `wa:i18n-ready`/`wa:locale-changed` — it had none, so the redraw hook was a silent no-op and the flag would have frozen at load-time locale. `defaultLocale()` added to `i18n.js` and the `window.__waI18n` seam. 19 refs repointed; browser suite pins the flag behaviour | `website/components/wa-locale-picker/v0/v0.1/v0.1.2/`, `website/components/wa-site-nav/v0/v0.1/v0.1.3/` | 2026-08-08 |
| `wa-locale-picker` v0.1.1 — the sgraph.ai pattern (Dinis): a compact trigger showing the current culture, opening a two-column panel of every culture, with flags. Flags are right HERE where they usually are not: the unit is a culture (language+country), so every entry names one country. Cultures we do not ship yet are listed dimmed and inert as SOON — the list is a roadmap, not a promise. Closes on toggle, click-away, Escape and selection (all four verified) | `website/components/wa-locale-picker/v0/v0.1/v0.1.1/` | 2026-08-08 |
| Four locales + the picker (issue 050, M2 first pass): `en-gb` (live), `en-us`, `pt-pt`, `pt-br` (draft) — each a folder with `core.json` (33 keys) + `culture.json`. pt-PT and pt-BR written as different CULTURES, not spellings (ficheiro/arquivo, ecrã/tela, and "áudio" vs "nota de voz" — what Brazilians actually search for). All declare GBP. `wa-locale-picker` v0.1.0: native names, NO flags, draft locales visible and marked. Automatic detection considers LIVE locales only — a draft is chosen, never assigned (a browser reporting en-US was silently served the unreviewed en-us before this was fixed); gated in `app-boot.test.mjs`. **Component strings still English** — only page copy localises | `website/app/locales/`, `website/components/wa-locale-picker/` | 2026-08-08 |
| Strings and culture are data (issue 050, M1b foundation + M1c): `website/app/i18n.js` is the whole runtime — key lookup, one-hop fallback to en-gb, `data-i18n` rendering, in-place `setLocale()` and `wa:locale-changed`; no framework. `locales/index.json` allowlists locales (selection may only name a key from it — issue-041 rule) and each locale is a FOLDER of per-domain files, so the files that exist are the support you have. `en-gb/core.json` (31 keys) drives 28 elements on the app page; `en-gb/culture.json` declares currency/rate/tone, and `fmtGbp` now delegates to `fmtMoney` reading it (GBP everywhere for now — a data change, not a refactor, when that changes). Format byte-identical: no `Intl.NumberFormat`, which would render £0.79 where the app renders £0.790. `scripts/check_locales.py` gates keys ⊆ en-gb, LIVE locales key-complete, draft locales may have holes, and every `data-i18n` key resolving. **Component internals (chat/flow/debug/errors domains, ~160 literals) NOT yet extracted** | `website/app/i18n.js`, `website/app/locales/`, `scripts/check_locales.py` | 2026-08-08 |
| The app's design is one swappable sheet (issue 050, M1a): `website/app/themes/default.css` declares all 45 `--wa-*` tokens on `:root`; `app.css` and all eight live component stylesheets read them and declare no colour of their own (157 literals replaced across nine files — before, only 3 of 8 components read a token at all). Tokens inherit through shadow DOM, verified: one `:root` override changes the computed value inside every component. Proven pixel-identical before/after (0.0000% on all eight qa-to-docs shots). `themes/index.json` is the allowlist; selection may only name a key from it, never a URL (issue-041 rule). Seven components bumped rather than edited in place (chat v0.1.2, debug v0.1.3, drop-zone/flow/key v0.1.1, rail/result-card v0.1.2) — component CSS is fetched unstamped, so a cached published version would have ignored the theme. Radius/spacing/type scale deliberately NOT tokenised — collapsing 7/8/9/10/12/14px radii moves pixels, so it needs its own reviewed change | `website/app/themes/`, `scripts/check_themes.py` | 2026-08-08 |
| Baselines are updated BY CI and UI change is RECORDED, not blocked (issue 053 — renumbered from 050 on merge; supersedes M-qtd-1's gate): `commit-baselines` (qa only) downloads the run's artifact, copies new + changed shots over the baselines, and appends `website/user-guide/baseline-changes.md` + `.json` — each entry naming the shots that moved, by how much, and the **commit + CI run** that moved them. A changed shot passes the run; a pixel threshold cannot tell a redesign from a regression, so an agent reviews the log (issue 051, NOT YET BUILT). Loop-safe: `GITHUB_TOKEN` pushes do not trigger workflows, plus `paths-ignore` on the screenshots path | `.github/workflows/qa-deploy.yml`, `scripts/record_baseline_changes.py`, `tests/unit/baseline-changes.test.mjs` | 2026-08-08 |
| i18n/culture/themes strategy (issue 050 — `PROPOSED — does not exist yet`, strategy doc only): three declarations (locales/cultures/themes) + minimal machinery for the app page, grounded in the SG/Send record (their 17-locale system and why it is off); M1 is extraction. Nothing implemented | `library/dev_packs/v0.1.22__app-i18n-culture-themes/00__strategy.md` | 2026-08-07 |
| **Tools section LIVE at `/tools/`** (issue 064): the site's home for small, single-purpose browser tools — what exists now, what is tool-shaped elsewhere on the site, and the five rules a tool here must meet (one job one page, no backend, the caller's own key, an API not just a UI, honest about cost). In the nav as **Tools ▾** (`wa-site-nav` **v0.1.8**), in `llms.txt`, in the sitemap, fetched by live-QA | `website/tools/index.html`, `website/components/wa-site-nav/v0/v0.1/v0.1.8/` | 2026-08-12 |
| **Text to speech, the first tool** (issue 064) at `/tools/text-to-speech/`: any text in, a spoken `.wav` out, entirely in the browser on the caller's own OpenRouter key (`sg-openrouter-mgmt-key`, the same one the app saves). Six voices; `core/sg-tts-openrouter` imported on FIRST USE so a visitor who only reads the page fetches none of it; pcm16 @24 kHz wrapped to WAV in-page; the cost READ BACK from the generation id, not estimated. Published updates are offered as starting scripts. The page states the whole workflow in four steps with nothing hidden. **Publishes `window.__tool` SYNCHRONOUSLY** during module evaluation (fixed 12 Aug after an agent's diagnostic found it undefined: it used to await `sg-tool-api.js` from the engine origin first, which took 1860 ms on a good path and never on a blocked one). A local implementation serves all seven actions with no network; the shared SgToolApi loads after and takes over the same name from one shared `ACTIONS` list. `window.__toolStatus` always exists — `{ ready, mode: 'local'|'sg-tool-api', methods, engine: {origin, loaded, error} }` — and the page states it visibly. With `manifest.json` and `skills/SKILL__api.md`: `synthesize`, `getVoices`, `setApiKey`, `hasApiKey`, `getLastAudio`, `saveLastAudio`, `newsScriptFor`. Audio crosses as base64 (a Blob does not survive `page.evaluate`); `saveLastAudio` triggers a real download Playwright captures. ONE `speak()` serves the button and the API, so there is no API version to drift. 6 browser tests / 30 assertions with `window.__ttsSynthesize` + `window.__ttsLookupCost` seams — no key, no network, no spend | `website/tools/text-to-speech/` | 2026-08-12 |
| `wa-voice-panel` v0.1.0 **RETIRED** (issue 064): the 🎙 pane on `/updates/` proved the approach on 11 Aug and was deleted on 12 Aug when the same job became a page. The Updates page keeps a sentence pointing at the tool; the tool keeps the posts-as-scripts convenience | deleted from `website/components/`; `scripts/templates/updates.html` | 2026-08-12 |
| Updates-as-audio brief (issue 062 — `PROPOSED — nothing built`): news-style TTS voice memos of each Journalist post via `core/sg-tts-openrouter`. Grounded findings: the module 403s at `tools.sgraph.ai` but serves 200 + CORS `*` from `dev.tools.sgraph.ai`; Node cannot import an https URL (`ERR_UNSUPPORTED_ESM_URL_SCHEME`) so the module must be vendored; no ffmpeg here and WAV is ~2.9 MB/min, so the encode step must be explicit. Proposes a Journalist-written `## Spoken` section, a deliberate generator with a declared USD ceiling and hash-skip, and a scripted-`fetchImpl` test that spends nothing | `library/briefs/updates-as-audio/`, `issues/open/062-…` | 2026-08-11 |
| Design candidates LIVE at `/design/` (issue 059): an A/B hub linking three themes (Studio/Console/Card) and four culture packs (en-gb/en-us/pt-pt/pt-br via `#code=`, plus `#view=diff` for the comparison matrix). Each runs the real flow — scripted fixtures with no key, the same code path with one. The `.dc.html` files are byte-identical to the hand-off and run on `website/design/support.js`, a runtime that exists ONLY under /design/ (no product code imports it). noindex, out of the sitemap, in llms.txt; `tests/design/render-check.mjs` gates them in both pipelines | `website/design/`, `tests/design/` Linked and explained from the site: nav (Engineering ▾ → Design candidates, `wa-site-nav` v0.1.8), a section on the Engineering hub, a Library card; live-QA fetches `/design/` | 2026-08-09 |
| Browser unit tests (issue 049, QUnit 2.24.1 vendored MIT): 11 tests / 35 assertions over workflow, config, audio-normalise (real File), debug-store (real localStorage), wa-site-nav (real element upgrade); the visual runner SHIPS with the site at `/tests/browser/` (linked from `/engineering/testing/`) and runs against the deployed modules; CI drives the same page headless in both workflows, reported as the `browser-unit` layer | `website/tests/browser/`, `tests/browser/run.mjs`, both workflows | 2026-08-07 |
| Go-live claims pass (issue 060, from Claude Design's `06-claims-audit.md` — thirteen untrue claims, all addressed): the three privacy chips DELETED and replaced by `core.routingHonest`, a plain statement that audio and transcripts go to OpenRouter and on to a provider we cannot name or control — deliberately NOT styled as a warning, because framing integrity as a hazard invites the reader to dismiss it; `core.modelCaveat` revealed WITH the transcript, never before the thing it qualifies exists; the home page's privacy-modes table replaced by "Where your audio goes", its pricing claims by "What it costs" (no plan, no credit, no card, no margin, and we do not set the price), and its hero/diagram/cards naming the four REAL artefacts — there is no "analysis" step, and "it works every time", "in seconds", "browser or app" and "your credits stay with you" are gone; a six-source "Getting the audio" section with the Meta non-affiliation line. **`/openrouter-key/`** is the guide nobody could use the product without: why BYOK, five steps with *set a spend limit* called out as the one that matters, and how to revoke — linked from the key panel itself, the nav, the hero, pricing, the footer, `llms.txt` and the sitemap. `wa-site-nav` v0.1.8 makes BETA persistent chrome (a page may change the WORD, never omit the fact) and the hero's "BETA — LIVE NOW" pill is gone. **The key is now asked for at RUN time**: a stranger loads a file, sets both options and reads the quoted maximum without meeting a password field, and the pass resumes by itself once a key is saved — gated on the COMPUTED display of `#key-section`, because `hidden` has lied here before. `wa-key-panel` v0.1.4 carries the guide link, and `wa-voice-panel` v0.1.1 carries the rename into the Updates' spoken lead-in — a new version rather than an edit, because v0.1.0 is published and immutable and that string is read OUT LOUD. The rename is complete on every page the site serves; the survivors are frozen component versions no page loads. The privacy-mode scheme was REMOVED from `concepts.json` (11 concepts, 2 schemes): the English pass had marked two of its chips `decide`, and the sign-off deleted the control rather than relabelling it — `_resolved` records that a review reached a verdict which was then overtaken by a better answer. NOT done and named as such: one `core.productName` key, and the `summary`→`debrief` interface rename | `website/index.html`, `website/openrouter-key/`, `website/app/`, `website/components/wa-site-nav/v0/v0.1/v0.1.8/`, `website/components/wa-key-panel/v0/v0.1/v0.1.4/` | 2026-08-12 |
| `tests/integration/demo.test.mjs` runs in BOTH pipelines. It was written with issue 063 and never added to a workflow — the one gate that proves the keyless demo touches no network was not being run by anything. Also widened `check_locales.py` rule 4 to walk `website/components`: component templates carry `data-i18n` keys and localise their own shadow DOM through the same `applyIn()`, so a key en-gb lacks renders the key string inside the component — the exact failure the rule exists to catch, in a directory it was not looking at | `.github/workflows/ci-pipeline.yml`, `.github/workflows/qa-deploy.yml`, `scripts/check_locales.py` | 2026-08-12 |

All rows above are committed and pushed in-session on their stated date.

## EXISTS (in sibling repos, reused not rewritten)

| Item | Where |
|------|-------|
| Audio-transcribe tool (working against the models) | `SGraph-AI__Tools/sgraph_ai_tools__static/tools/.../en-gb/audio-transcribe/` |
| sg-audio-decode core | `SGraph-AI__Tools/sgraph_ai_tools__static/core/sg-audio-decode/` |
| Infographic generator (OpenRouter, key in localStorage, live users) | `SGraph-AI__Tools` / tools.sgraph.ai |
| LLM component library (`sg-llm-*`), sg-layout | `SGraph-AI__Tools` |
| Google social login implementation | prior SGraph project (per 27 Jul arch brief) |
| Mobile deployment path incl. Apple/Android signing | prior SGraph project (per 27 Jul arch brief) |

## PROPOSED — does not exist yet

- Analysis/debrief outputs beyond the summary document (if ever wanted — the arch brief's fuller set).
- iOS and Android targets; Chrome extension (deliberately later).
- OpenRouter per-user key provisioning; the key-provisioning Lambda; the beta hardcoded key.
- Privacy mode selector (routed / restricted / browser-local); browser-local engine.
- Credit purchase flow (Stripe), minimum top-up £5/£10; accounts (social login / local storage).
- Everything else in the 27 July arch brief's open-questions table.

## Decision Log

| Date | Decision | Source |
|------|----------|--------|
| 2026-07-27 | No backend; static hosting (GitHub Pages / S3+CloudFront) | 27 Jul arch brief |
| 2026-07-27 | OpenRouter carries inference and billing; margin = key credit limit | 27 Jul arch brief |
| 2026-07-27 | Privacy as three selectable modes | 27 Jul arch brief |
| 2026-07-27 | 90-day term from 1 Aug 2026; 25% of net; 50/50 profit split | 27 Jul strategy brief / contract draft |
| 2026-07-28 | Default branch is `dev`; two-branch CI with auto-tag | Dinis, session instruction |
| 2026-07-28 | Team/library/CI conventions mirrored from `__App__Send` and `__Tools` | Dinis, session instruction |
| 2026-07-28 | Terms with the partner agreed; work is open source and published as produced | brief v0.33.53 |
| 2026-07-28 | First milestone = the end-to-end branded experience; results stream in arrival order (progress → transcript → summary → infographic) | brief v0.33.53 |
| 2026-07-28 | Privacy tiers to be enforced server-side via OpenRouter guardrails on the issued key (model / provider / data policy), not client logic alone | brief v0.33.53 (finding) |
| 2026-07-28 | Hosting: GitHub Pages behind an AWS Route 53 domain | brief v0.33.53 |
| 2026-07-28 | Second administrative vault, separate from user-facing; the ciphertext rule added to `.claude/CLAUDE.md` and needs pre-commit enforcement | brief v0.33.53 |
| 2026-08-05 | `window.__tool` identity: our page always publishes its own `whatsapp-transcribe` SgToolApi; if the spike picks Attempt 1, the upstream engine handle is captured and wrapped (brief 05 wins; resolves review gap 1) | Dinis, session decision |
| 2026-08-05 | UI currency is GBP, converted from the engine's USD metering at a fixed versioned rate in app config, with a "metered in USD" note (resolves review gap 3) | Dinis, session decision |
| 2026-08-05 | M1 key entry: user pastes their own OpenRouter key on the page; stored in localStorage (BYOK; resolves review gap 4) | Dinis, session decision |
| 2026-08-05 | LLM prompts (summary, infographic) live as markdown files served from the site and fetched at runtime — editable without code changes (extends review action 4) | Dinis, session decision |
| 2026-08-05 | Scope confirmed: "tools.sgraph.ai parity" = the capabilities THIS product needs (the one-pass job, starting from a WhatsApp voice memo) — Live/TTS/Chat stay out of v1 | Dinis, session decision |
| 2026-08-06 | OpenRouter account: personal, for now | Dinis, review-pack decision D1 |
| 2026-08-06 | Model allowlist fixed to the five models the app calls (gemini-3.5-flash, the two gemini image models, claude-sonnet-4.5, gpt-5-mini); key guardrails to restrict to it — Dinis also configuring directly on OpenRouter | Dinis, review-pack decision D2; AppSec review 08/06 |
| 2026-08-06 | Licences settled: code Apache 2.0 (LICENSE canonical), written material CC BY 4.0 — closes the contract draft's licence open point | Dinis, review-pack decision D3 |
| 2026-08-06 | Branch topology: `dev` and `main` go to the same place for now; `dev` is treated as production (it publishes the live domain). A separate `main` estate is a future split | Dinis, review-pack decision D4 |
| 2026-08-06 | Issue 007 closed as superseded: the seven 28-Jul strategy infographics never landed (files' whereabouts unknown) and the three 31-Jul product infographics cover the library's needs | Dinis, session decision |
| 2026-08-08 | Screenshot baselines overwrite on change and are logged against the commit, rather than failing the build — "UX changes are to be expected, what we need to keep an eye on is if the change happened in the correct place, but that needs to be done via an agent and not a CI pipeline" | Dinis, session decision (issues 050/051) |
| 2026-08-06 | `?origin=` removed rather than allow-listed — "the solution is not to have that feature at all, since we can hardcode that origin"; two branches had independently allow-listed it the same day | Dinis, session decision (issue 041) |
