# Debrief: The Review Pack Executed — Groups A, B1–B2, C and G In One Session

**version** v0.1.21
**date** 6 August 2026
**from** Claude (execution session agent)
**to** Dinis, the team
**type** Session debrief

*Part of the [project library](https://whatsapp-voice-transcription.sgraph.ai/library/) — every doc behind this product, organised by audience.*

---

Same-day follow-through on the v0.1.20 review pack: Dinis asked for groups A, B1–B2
and the two proposals in sequence. Four commits on
`claude/project-review-brief-packs-oxtw7n`, each self-contained and tested.

## 1. Group A — the record fixed (`5e890ab`)

CHANGELOG's Unreleased block split into its real tag headings v0.1.12–v0.1.21 (every
tag now has a dated heading with a pinned compare link, including M1's restored
entry); `versions.json` caught up with three new entries; the Updates page's 404'd
issue-034 link and `blob/qa` links repointed, the two moving `...dev`/`...qa` diffs
pinned to tags; reality-doc and issue-README contradictions closed; both dev packs'
PROPOSED lines marked SHIPPED; open-issue status notes re-dated (007 nudged:
supply the seven images or close as superseded — **a Dinis decision**); landing copy
made honest (no "pick your privacy mode" step, pricing split into today-BYOK vs
PROPOSED credits, tense fixed, OpenRouter named). The live-QA job now link-checks
Updates/Library — validated locally, it caught the exact 404 the commit fixes.

## 2. Group B1–B2 — the urgent security slice (`adc65b4`, issue 037)

`?origin=` is allowlisted (S1 — the review's highest-priority finding, closed;
unit-tested including suffix-spoof and `javascript:` cases) and the app page carries
a meta CSP (S2) pinning script/connect sources. The CSP was validated against the
full REAL decode chain — the jsdelivr opus decoder's blob-URL import and inlined
WASM need `blob:` + `'wasm-unsafe-eval'`, discovered by running a fixture through
`sg-audio-decode` under the policy — and both integration gates. En route:
`scripts/mirror_engine.mjs` (review E4) builds the local engine mirror the
integration tests' `MIRROR_DIR` mode needs for sandboxed runs. S3–S5 remain open.

## 3. Group C — the open engineering hub (`c6bf827`, issue 036)

M-hub-1 + M-hub-2 shipped: `/engineering/` and five section pages, all rendering
CI-emitted JSON (`emit_engineering_json.py`: status + issue queue + doc inventory;
per-layer test results travel from the test job by artifact). Each page is
principle → live view → receipts; the security page doubles as the interim trust
page. M-hub-3 partially: OG tags + RSS feed shipped; the entrepreneur series
remains open (Journalist cadence). All six pages render-checked headless with real
data and are in the live-QA check.

## 4. Group G — QA-to-docs (`71e4337`, issue 038, M-qtd-1)

Two journeys (the one pass; the chat), 7 manifest-named screenshots with masks, a
pixelmatch diff gate, wired into both workflows as a gating layer. Proven both
directions in-session: 0.000% self-diff on rerun (determinism), a perturbed
baseline caught at 0.954%. **Baselines arm on the first CI run** (CI-only baseline
policy): the run will pass with "candidate captured" warnings and upload 7
candidates as the `qa-to-docs` artifact — review and commit them to
`website/user-guide/screenshots/`. M-qtd-2 (user-guide pages) and M-qtd-3
(auto-refresh) remain open.

## Needs a human

1. **Issue 007** — supply the seven infographic images or close as superseded.
2. **First-CI-run baselines** — commit the 7 candidate screenshots when the run
   uploads them (any agent session can do it once the artifact exists).
3. The still-open security items S3–S5 (issue 037) are the next highest-value
   engineering hours; B4 (secrets scanner, issue 013) is on the key-era critical path.

---

This document is released under the Creative Commons Attribution 4.0 International licence (CC BY 4.0).
